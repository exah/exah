import styled from '@emotion/styled'

export const ButtonBase = styled('button')`
  appearance: none;
  font: inherit;
  color: inherit;
  background: none;
  border: none;
  padding: 0;

  display: block;
  cursor: pointer;
`
